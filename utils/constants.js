const DBTABLES = {
    users: 'USERS',
    auth: 'CREDENTIALS',
    groups: 'GROUPS',
};

const statusResponse = {
    403: 'Forbidden request'
};

module.exports = {
    DBTABLES,
    statusResponse
};